# `/sicario.threatmodel`

Create or update `docs/security/threat-model.md` from the active spec and plan.

Required coverage:

- assets
- entry points
- trust boundaries
- abuse actors
- prompt injection if AI is in scope
- unsafe external write if external systems are in scope
- human approval boundaries
- residual risks

The output is a draft until reviewed by a human.

