# Contributing

Contributions should follow SicarioSpec's own guardrails.

Before opening a PR:

```bash
python3 -m unittest discover -s tests
python3 -m sicario_cli.cli verify .
```

Update docs or record a docs impact decision for every behavior change.

