{
  "id": "SICARIO-HARDCODED-PROVIDER-TOKEN",
  "severity": "critical",
  "kind": "regex-forbidden",
  "path": "**/*",
  "params": {
    "pattern": "(?<![A-Za-z0-9])sk-[A-Za-z0-9_-]{20,}"
  },
  "message": "Provider API token (sk- prefixed) committed to the repository",
  "enabled": true
}
