{
  "id": "SICARIO-HARDCODED-AWS-KEY",
  "severity": "critical",
  "kind": "regex-forbidden",
  "path": "**/*",
  "params": {
    "pattern": "(?<![A-Za-z0-9])AKIA[0-9A-Z]{16}"
  },
  "message": "AWS access key id committed to the repository",
  "enabled": true
}
