# Abuse Cases

- A developer initializes SicarioSpec into the wrong directory.
- A user tries to force overwrite existing governance files accidentally.
- A generated spec for an AI agent omits prompt injection controls.
- A repo includes a hardcoded token and attempts to pass verification.
- A feature changes behavior without docs or diagram updates.

